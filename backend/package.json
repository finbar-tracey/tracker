{
  "name": "gym-tracker-backend",
  "version": "1.0.0",
  "description": "Backend API for Gym Tracker - Garmin and Whoop integration",
  "main": "index.js",
  "scripts": {
    "dev": "netlify dev",
    "build": "netlify build",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
